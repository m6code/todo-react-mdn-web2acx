import React from 'react'

const SearchField = () => {
    return (
            <input type="text" aria-label="Enter a new task" placeholder="What needs to be done?" className="inputField" />
    )
}
export default SearchField;
